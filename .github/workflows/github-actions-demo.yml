name: Trigger test
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches:
      - main
      - develop

env:
  # CLIENT_ID: ${{ secrets.CLIENT_ID }}
  # CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
  CLIENT_ID: xaIkb2y6ASAKJ15sfJYLZFKsxO9uoWZ2mOtXNEepyTk=
  CLIENT_SECRET: 001bf08a7cab4aaa28b8ad6d9eae4e4aea909b44e4aad1f431d2ec1e19a9b8ca
jobs:
  Trigger-Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: export NODE_OPTIONS="--max-old-space-size=3072"
      - run: yarn install
      - run: yarn build
      # - run: apt update && apt install -y gettext
      - run: echo "success"
      - run: echo $CLIENT_ID
      - run: echo $CLIENT_SECRET
      - run: token=$(curl -H "Content-Type:application/json" -X POST --data "{\"clientId\":\"$CLIENT_ID\",\"clientSecret\":\"$CLIENT_SECRET\"}" https://kiet-le-dss-macbook-air.tail305ff.ts.net/api/v1/apikeys/authenticate| tr -d '"')
      - run: echo $token
